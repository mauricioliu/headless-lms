// waves context — the Ola: a group of Trabajadores inscribed together in a
// Curso. Owns the group and its membership; the person is identity's, the org
// link is organizations', the access grant is entitlements'.
export type {
  Wave,
  WaveMember,
  WorkerRow,
  IngestWaveInput,
  IngestWaveResult,
  WaveReport,
  WaveReportTotals,
  WaveWorkerReport,
  WaveWorkerEvaluationStatus,
} from './schemas/waves.js';
