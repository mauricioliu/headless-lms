// entitlements context — domain entities & DTOs, owned by @headless-lms/core/types.
export type {
  EntitlementStatus,
  Entitlement,
  EntitlementsQuery,
  GrantEntitlementInput,
  Page,
} from '../types/index.js';
