// organizations context — domain events, owned by @headless-lms/types.
export type {
  OrganizationEvent,
  InviteCreated,
  InviteCanceled,
  InviteAccepted,
  StudentCreated,
  StudentDeleted,
} from '@headless-lms/types';
