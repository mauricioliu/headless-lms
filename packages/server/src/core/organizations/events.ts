// organizations context — domain events, owned by @headless-lms/types.
export type {
  OrganizationEvent,
  InvitationCreated,
  InvitationCanceled,
  InvitationAccepted,
  StudentCreated,
  StudentDeleted,
  StudentLinked,
} from '@headless-lms/types';
