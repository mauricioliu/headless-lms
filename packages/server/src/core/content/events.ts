// content context — domain events, owned by @headless-lms/types.
export type {
  CourseCreated,
  CourseUpdated,
  CourseDeleted,
  DownloadCreated,
  DownloadUpdated,
  DownloadDeleted,
  ContentEvent,
} from '@headless-lms/types';
