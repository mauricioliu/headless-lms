// discussion context — domain entities, owned by @headless-lms/types.
export type {
  Comment,
  CommentAuthor,
  CommentStatus,
  CommentReaction,
  CommentReport,
  DiscussionSettings,
  ActivityThreadState,
  ThreadState,
} from '@headless-lms/types';
