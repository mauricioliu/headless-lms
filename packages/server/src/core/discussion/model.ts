// discussion context — domain entities, owned by @headless-lms/types.
export type {
  Comment,
  CommentAuthor,
  CommentStatus,
  CommentReaction,
  CommentReport,
  CommentSettings,
  CommentsState,
} from '@headless-lms/types';
