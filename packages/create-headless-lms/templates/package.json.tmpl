{
  "name": "{{NAME}}",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/main.ts",
    "build": "tsdown",
    "start": "node --env-file=.env dist/main.js",
    "migrate": "headless-lms migrate",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@headless-lms/adapter-storage-minio": "{{STORAGE_MINIO_RANGE}}",
    "@headless-lms/cli": "{{CLI_RANGE}}",
    "@headless-lms/core": "{{CORE_RANGE}}",
    "@headless-lms/server": "{{SERVER_RANGE}}"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsdown": "^0.22.9",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
